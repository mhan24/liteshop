# Changelog

## 0.3.0 — Earth（地球）· remade

这是 LiteShop 的重制版正式发布，重点完成订单安全、支付一致性和运维恢复能力加固。

### 安全与权限

- 订单查看改为必须使用邮件下发的 token，邮箱不再作为订单访问凭证。
- viewer 订单/商品响应脱敏，隐藏成本、联系方式、支付地址、交易号和发货内容。
- 卡密、订单详情和订单导出收紧到 operator 及以上角色。
- 密码修改后吊销旧会话并签发新会话。
- 健康检查详细信息仅 admin 可见。
- 首次初始化需要 bootstrap token，防止空库被远程抢先接管。
- Turnstile 密钥无法解密时失败关闭。
- 公网 HTTP 支付、站点和通知 URL 被拒绝。

### 支付与订单一致性

- 支付回调必须匹配订单所属网关并包含交易号。
- 取消/过期前先确认远端支付交易状态。
- 本地落库失败时尝试取消已创建的远端交易并触发告警。
- 订单状态迁移使用旧状态条件更新，避免并发覆盖。
- 防止管理员手动状态接口绕过支付、库存和发货专用流程。
- 防止终态订单补发时回退状态或误扣库存。
- 商品金额计算增加 NaN、Inf 和整数溢出保护。

### 可靠性与恢复

- 支付、发货和人工交付事件写入 Outbox，支持崩溃恢复。
- 同步通知正确传播 Telegram/Webhook 失败。
- 邮件失败进入持久化重试队列，队列写入失败不再静默吞掉。
- 系统重置清理优惠券、审计、低库存提醒等遗漏数据，同时保留数据库加密根密钥。
- 配置恢复重新校验 URL 和支付回调路径。

### 运维与导出

- CSV 导出使用标准字段转义，降低公式注入和错列风险。
- 更新 OpenAPI 文档、前台初始化流程和回归测试。
