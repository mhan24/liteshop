run:
  timeout: 5m
linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
  disable:
    - gosimple
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
